import { createResource, For, on, Show, type Component } from 'solid-js';
import styles from './Home.module.css';

const Home: Component = () => {
  return (
    <section>
        <h1>Home</h1>
    </section>
  );
};

export default Home;
